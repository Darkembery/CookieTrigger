//if spawn_delay > 0
//spawn_delay--

if number_of_spawns >= 75
{
	spawn_delay = 80
}

else if number_of_spawns == 60
{
	spawn_delay = 90
}

else if number_of_spawns == 30
{
	spawn_delay = 95
}

else if number_of_spawns >= 15
{
	spawn_delay = 100
}